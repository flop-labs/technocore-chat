__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.coverage
.venv/
# pywrangler builds these under mcp/worker: a CPython venv for its own CLI, a Pyodide one
# for the wasm target, and the resolved wheel set it bundles. All three are machine- and
# toolchain-specific and are rebuilt by `pywrangler dev`.
.venv-workers/
pylock.toml
data/
dist/
node_modules/
mutants/
.schemathesis/

# local agent workspaces and scratch worktrees
.claude/
.codex/
.entire/
.gemini/
.opencode/
.pi/
.worktrees/

# local Quint oracle work in progress — never commit from this machine
quint-*
